# Core (kleinanzeigen + immoscout24 mobile API + LLM + telegram) — all stdlib + these:
pyyaml>=6.0
feedparser>=6.0.10
anthropic>=0.40.0   # only needed if llm_backend: api (cli backend uses `claude -p`, no dep)

# Optional — only for the (currently disabled) Immowelt Playwright scraper:
# playwright>=1.45.0   then: playwright install chromium
